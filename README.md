# Dancing ASCII Bear App

A simple web application featuring a dancing ASCII bear. The bear starts dancing automatically on load, with a Start/Stop button so the bear can rest when needed.

## Decisions

- **Stack:** Vanilla HTML/CSS/JS — single `index.html`, no dependencies, no build step
- **Bear design:** 4-frame ASCII animation sourced from [node-dancing-bear](https://github.com/joeyism/node-dancing-bear)
- **Bear color:** Yellow (`#FFD700`) on a dark background
- **Frame rate:** 150ms per frame (~6.7 fps)
- **Deployment:** Local only (open `index.html` directly in a browser)

## Usage

Just open `index.html` in any browser. No server or install needed.

## Tasks

- [x] Define app requirements
- [x] Choose tech stack (vanilla HTML/CSS/JS)
- [x] Select ASCII bear frames (4-frame cycle)
- [x] Implement dancing animation with Start/Stop control
- [x] Apply yellow color per Mahesh's requirement
- [x] Initialize git repository
- [x] Push to GitHub

### Outstanding

- [ ] Tune frame rate (currently 300ms — consider faster animation)
- [ ] Add responsive design for mobile devices
- [ ] Make the bear green instead of yellow

